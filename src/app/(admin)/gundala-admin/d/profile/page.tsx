import { AccountSettingWrapper } from '@/features/auth/components/account-setting-wrapper';

export const metadata = {
	title: 'My Profile',
};

export default function ProfilePage() {
	return <AccountSettingWrapper />;
}
